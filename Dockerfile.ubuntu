from ubuntu:mantic

run apt update && apt install -y ca-certificates golang
workdir /root
run git clone https://github.com/femnad/fup.git
workdir /root/fup
run go install
entrypoint ["/root/go/bin/fup", "-f", "contrib/test.yml", "-b"]
